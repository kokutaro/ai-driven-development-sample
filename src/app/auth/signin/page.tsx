import { SignInPage } from '@/components/auth'

/**
 * サインインページ
 *
 * OAuth認証プロバイダーを使用したサインインページを表示します。
 * - Google OAuth
 * - GitHub OAuth
 * - Microsoft OAuth
 */
export default function SignIn() {
  return <SignInPage />
}
